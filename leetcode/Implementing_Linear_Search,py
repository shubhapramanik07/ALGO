

nums = [4,1,8,7,6,5,2,3,9,0]
target = 5
def linear_search(nums, target):
    for i in range(len(nums)):
        if nums[i] == target:
            print("the targeted value is in the index",i)
        
linear_search(nums,9)

# time complexity: O(n)
# space complexity: O(1)